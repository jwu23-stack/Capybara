import React from 'react';

export function CategoryPage(props) {
    // Pull down list of subcategories corresponding to the category that was passed as a prop
    return (
        // Populate with cards that contain that subcategory information
        <div></div>
    )
}