import React from 'react';

// Props: Category name (as it appears in Firebase)
export function CategoryPage(props) {
    // Pull down list of subcategories
    // For each <subcategory> in the <category>
        
    return (
        // Populate with cards that contain that subcategory information
        <div></div>
    )
}